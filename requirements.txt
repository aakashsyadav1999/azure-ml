# Core ML libraries for local development and training
pandas>=2.0.0,<2.3.0
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0.0
joblib>=1.3.0
pyyaml>=6.0
azure-ai-ml>=1.30.0
azure-identity>=1.15.0
requests>=2.31.0
mlflow>=2.8.0

# Azure ML SDK (for pipeline submission)
# Note: These are installed separately in GitHub Actions due to dependency conflicts
# azureml-core>=1.56.0
# azureml-dataset-runtime>=1.56.0
# azureml-defaults>=1.56.0

# Visualization (optional)
matplotlib>=3.6.0

# Development tools (optional)
pytest>=7.4.0